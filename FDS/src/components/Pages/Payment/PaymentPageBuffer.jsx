import React from 'react'
import { Outlet } from 'react-router-dom'

function PaymentPageBuffer(props) {
    return (
        <div>
            <Outlet />
        </div>
    )
}

export default PaymentPageBuffer

